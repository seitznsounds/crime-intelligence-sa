import RecoveryClient from "./RecoveryClient";

export const metadata = {
  title: "Asset Recovery Tracker | Crime Intelligence SA",
  description: "Live dashboard tracking state asset forfeitures and criminal property seizures.",
};

export default function RecoveryPage() {
  return <RecoveryClient />;
}
