import ResearchClient from "./ResearchClient";

export const metadata = {
  title: "Deep Research Portal | Crime Intelligence SA",
  description: "Trigger autonomous investigative research to connect disparate corruption nodes."
};

export default function ResearchPage() {
  return <ResearchClient />;
}
